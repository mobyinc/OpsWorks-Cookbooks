<% pid_file = "#{@deploy[:deploy_to]}/shared/pids/#{@application}_api.pid" %>
<% environment_variables = @deploy[:environment_variables].map { |key, value| "#{key}=#{value}" }.join(' ') %>

check process <%= @application %>_api
  with pidfile <%= pid_file %>
  start program = "/bin/su - <%= @deploy[:user] %> -c 'cd <%= @deploy[:current_path] %>/jobs && <%= environment_variables %> bundle exec rackup -p 4567 -o 0.0.0.0 -P <%= pid_file %>'" with timeout 90 seconds
  stop  program = "/bin/su - <%= @deploy[:user] %> -c 'kill -s TERM `cat <%= pid_file %>`'" with timeout 90 seconds
  group api_group
